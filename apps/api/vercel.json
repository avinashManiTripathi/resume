{
    "version": 2,
    "buildCommand": "cd ../.. && npx turbo run build --filter=api",
    "installCommand": "cd ../.. && npm install",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ]
}