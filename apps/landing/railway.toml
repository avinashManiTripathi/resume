[build]
builder = "NIXPACKS"
buildCommand = "npx turbo run build --filter=landing..."

[deploy]
startCommand = "cd apps/landing && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
