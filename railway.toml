[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start --workspace=landing"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Force HTTPS redirect
[deploy.healthcheck]
path = "/"
port = 3000

# Environment-specific settings
[settings]
forceHttps = true
